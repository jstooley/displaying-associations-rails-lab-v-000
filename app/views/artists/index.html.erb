<% @artists.all do |artist|%>
<%= link_to(artist.name, artist_path(artist)) %>
<p> Songs: </p>
  <% artist.songs.each do |song|%>
    <p><%= link_to(song.title, song_path(song)) %></p>
  <% end %>
<% end %>
